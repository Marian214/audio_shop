<h2>Редагувати <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field" style="margin-bottom: 20px;">
    <%= f.label :email, "Електронна пошта" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Наразі очікується підтвердження для: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field" style="margin-bottom: 20px;">
    <%= f.label :password, "Новий пароль" %> <i>(залиште порожнім, якщо не хочете змінювати)</i><br />
    <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
    <% if @minimum_password_length %>
      <br />
      <em>(мінімум <%= @minimum_password_length %> символів)</em>
    <% end %>
  </div>

  <div class="field" style="margin-bottom: 20px;">
    <%= f.label :password_confirmation, "Підтвердження пароля" %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
  </div>

  <div class="field" style="margin-bottom: 20px;">
    <%= f.label :current_password, "Поточний пароль" %> <i>(нам потрібен ваш поточний пароль для підтвердження змін)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password", class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "Оновити", class: 'btn btn-primary' %>
  </div>
<% end %>

<h3>Скасувати мій акаунт</h3>

<div>Не задоволені? <%= button_to "Скасувати акаунт", registration_path(resource_name), data: { confirm: "Ви впевнені?", turbo_confirm: "Ви впевнені?" }, method: :delete, class: 'btn btn-danger' %></div>

<%= link_to "Назад", :back, class: 'btn btn-secondary' %>
